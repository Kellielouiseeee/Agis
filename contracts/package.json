{
  "name": "agis-contracts",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:simple": "hardhat run scripts/deploy_simple.ts --network localhost"
  },
  "devDependencies": {
    "hardhat": "^2.17.0",
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@openzeppelin/contracts-upgradeable": "^4.9.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
